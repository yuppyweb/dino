// Code generated by dino. DO NOT EDIT.
// for testing purposes only.

package dino

import (
	"reflect"
)

// MockRegistry returns the registry used by the Dino container.
func (d *Dino) MockRegistry() Registry {
	return d.registry
}

// MockRegister directly stores a value in the registry for testing purposes.
func (r *SyncMapRegistry) MockRegister(key RegistryKey, value any) {
	r.sm.Store(key, value)
}

// MockIsStruct reports whether rt is a struct type.
func MockIsStruct(rt reflect.Type) bool {
	return isStruct(rt)
}

// MockIsPointerToStruct reports whether rt is a pointer to a struct type.
func MockIsPointerToStruct(rt reflect.Type) bool {
	return isPointerToStruct(rt)
}

// MockIsFunction reports whether rt is a function type.
func MockIsFunction(rt reflect.Type) bool {
	return isFunction(rt)
}

// MockIsNil reports whether rv is nil or invalid.
func MockIsNil(rv reflect.Value) bool {
	return isNil(rv)
}

// MockAsError extracts an error from rv if it implements the error interface and is not nil.
func MockAsError(rv reflect.Value) error {
	return asError(rv)
}
